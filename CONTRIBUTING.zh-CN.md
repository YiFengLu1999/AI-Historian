# 参与 AI Historian

**中文** | [English](CONTRIBUTING.md)

我们热烈欢迎来自数字人文、历史研究、自然语言处理和 Agent 系统工程社区的贡献。

## 适合参与的方向

- 实验一与实验二冻结材料的复现检查
- runtime scope 检索与 `episode_packet` 排序
- 跨文档证据核验与时间约束传播
- 历史历法转换与时间规范化
- 语料适配、元数据目录与来源追踪
- 测试、文档、示例和易用性改进

## Issue 信息

建议提供：

1. 使用的执行 profile：`evaluation`（`aih`）或 `scalable_fulltext`（`aih-fulltext`）；
2. 运行命令及相关环境变量名称；
3. 涉及的输入与输出路径；
4. 预期行为与实际观察；
5. 可分享的精简示例或材料索引。

## 命名约定

- 面向读者的目录、文档、整理后的输入、图表和最终结果材料统一使用小写 `kebab-case`。
- `README.md`、`LICENSE.md`、`CITATION.cff`、`CONTRIBUTING.md` 等约定文件，以及 `H-C1`、`T1` 等正式实验编号保持原样。
- Python 模块与脚本、其他源代码文件、JSON 字段、schema 标识和运行时生成的工作文件使用 `snake_case`。
- 冻结原始输出树中的名称如果属于生成流水线的复现契约，则保持原样；这些内部名称不作为新增公开路径的命名范例。

## Pull request

建议保持改动范围清晰，记录面向用户的行为变化，并为可复用逻辑添加确定性测试。涉及论文指标的改动请同时提供来源材料、聚合规则和修改前后数值。

感谢您帮助 AI Historian 变得更加透明、可复现并服务于真实历史研究。期待您的想法，也期待一起合作。
